-- begin actions.lua
--
local wezterm = require("wezterm")
local module = {}

function module.apply_to_config(config)
	--define actions
	--
	--rename tab to "${PWD##*/}"
	--
	--rename tab to given string
	--
	--open default workspace
	--
	--open rails workspace
	--
end

return module
