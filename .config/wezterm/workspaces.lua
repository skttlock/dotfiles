-- begin workspaces.lua
--
local wezterm = require("wezterm")
local module = {}

function module.apply_to_config(config)
	-- define default workspace
	--
	--
	-- define rails workspace
end

return module
